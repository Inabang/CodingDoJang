// #include <stdio.h>

// int main()
// {
//   int *numPtr; // 포인터 변수선언.
//   int num1 = 10;

//   numPtr = &num1; // num1의 메모리 주소를 포인터 변수에 저장.

//   printf("%p\n", numPtr);  // 포인터 변수 numPtr의 값을 출력한다
//   printf("%p\n", &num1);  // 변수 num1의 메모리 주소를 출력한다.
// }

// 포인터 변수를 선언할 때는 자료형 뒤에 * 를 붙인다.
// *의 위치에따른 차이는없다.

// 포인터 변수를 선언했으면 다음과 같이 &로 변수의 주소를 구해서 포인터 변수에 저장한다.
// numPtr = &num1; => num1의 메모리 주소를 포인터 변수에 저장.