// #include <stdio.h>

// int main()
// {
//   int num1 = 10;

//   printf("%p\n", &num1);
//   // 메모리의 주소를 출력한다.
//   // 컴퓨터마다, 실행할 때마다 달라짐.
//   // 변수의 메모리 주소를 구할 때는 변수 앞에 & (주소 연산자)를 붙인다.

//   // 하모니카KR은 리눅스 환경이기때문에
//   // 메모리주소는 앞에 0x가 붙고 A~F는 소문자로 출력된다 
//   // 또한 높은 자릿수의 0은 생략된다.
//   return 0;
// }