typedef 로 자료형의 별칭


구조체의 멤버에 접근할때는 .를 사용한다.

구조체 포인터에는 &(주소연산자)로 구조체 변수의 메모리 주소를 구해서
할당할 수 있다.

ptr = &struct;


비교연산자로는 두 구조체의 내용이 같은지 알수없다.

이때는 어쩔수 없이 구조체 변수의 모든 멤버를 비교해야한다.
구조체 포인터로 구조체의 메모리 주소를 할당해서
비교를하면 같은지 비교할수잇다.

