// #include <stdio.h>

// int main()
// {
//   char s1[10] = "Hello";

//   printf("%s\n", s1);

//   return 0;

//   // 크기가 10인 char형 배열을 선언한뒤 문자열을 할당했다
//   // 문자열 리터럴을 포인터에 할당하는 방식과 달리 문자열을 배열에 저장하는 방식은
//   // 배열 요소 하나 하나에 문자가 저장된다. 즉 배열 안에 일렬로 나열된 문자가 모여
//   // 문자열을 이루게 된다. 문자열 맨뒤에는 NULL이들어간다 (남는공간에도)
// }