// #include <stdio.h>

// int main()
// {
//     if (2)
//         printf("참\n");
//     else
//         printf("거짓\n");

//     return 0;
// }


// 0이아닌 양수와 음수를 넣으면 무조건 True이다.
// 실수도 마찬가지로 0.0f는 False이며 나머지는 모두 True다.

// if조건식에 num1 = 10 처럼 num1에 10을 지정하면 if는 num1에 10이 들어있기에
// if문안의 코드를 출력한다 만약 0으로 지정했다면 False로 인식해 else를 출력하거나
// 아무것도 출력이 되지 않는다.


// 만약 실수를 방지하고 싶다면 num == 10 대신에 10 == num1 형식으로
// 반대로하면된다 이렇다면 =를 한개쓴다면 리터럴형식에 값을 지정할수없기에
// 컴파일 에러가뜬다.