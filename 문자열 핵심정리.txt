문자열

문자열은 "" 큰따옴표로 묶으며 char 포인터나 배열에 저장하여 사용한다.

char *s1 = "String String!!"
char s2[20] = "Hello String"
char s3[] = "Helo String Array"

문자열을 인덱스로 접근하면 문자를 출력할수있다

이미 선언된 배열에는 문자열을 할당할수 없다

문자열 포인터에 문자열 리터럴을 할당한 뒤에는 
인덱스로 접근하여 문자(요소) 를 변경할수 없다.

하지만 문자배열은 인덱스로 접근하여 문자를 변경할수 있다.
